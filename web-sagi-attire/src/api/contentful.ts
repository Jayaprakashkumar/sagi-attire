import { SPACE_ID, ACCESS_TOKEN } from "../contentful/contentfulClient";

export const CONTENTFUL_URL = `https://graphql.contentful.com/content/v1/spaces/${SPACE_ID}`;
export const CONTENTFUL_ACCESS_TOKEN = `Bearer ${ACCESS_TOKEN}`;
